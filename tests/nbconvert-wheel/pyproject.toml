[tool.poetry]
name = "test-nbconvert-wheel"
version = "0.1.0"
description = "Test of wheel for nbcovert"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.9"
nbconvert = "^7.2.6"

[build-system]
requires = ["poetry-core>=1"]
build-backend = "poetry.core.masonry.api"
