[tool.poetry]
name = "fiona-source"
version = "0.1.0"
description = "Test of fiona source"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.9"
fiona = ">=1.9"

[build-system]
requires = ["poetry-core>=1"]
build-backend = "poetry.core.masonry.api"
