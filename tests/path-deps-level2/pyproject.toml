[tool.poetry]
name = "path-deps-level2"
version = "0.1.0"
description = "poetry2nix test"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.6"
path-deps = { path = "../path-deps" }

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
