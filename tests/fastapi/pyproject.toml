[tool.poetry]
name = "test-fastapi"
version = "0.1.0"
description = "Test of python fastapi package"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.9"
fastapi = "^0.111"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
