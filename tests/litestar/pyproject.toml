[tool.poetry]
name = "test-litestar"
version = "0.1.0"
description = "Test of python litestar package"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.9"
litestar = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
