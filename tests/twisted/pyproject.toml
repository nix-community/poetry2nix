[tool.poetry]
name = "test-twisted"
version = "0.1.0"
description = "Test of python twisted package"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.6"
twisted = "^24.3.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
